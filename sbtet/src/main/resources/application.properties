



# Server
server.port=8080

# Database
spring.datasource.url=jdbc:mysql://localhost:3306/sbtetattandance?useSSL=false&serverTimezone=Asia/Kolkata&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=sanath1234567
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT
jwt.secret=sbtet_attendance_super_secret_key_change_in_production_must_be_32_chars_min
jwt.expiration.ms=604800000

# Cloudinary
cloudinary.cloud-name=dswig7bh1
cloudinary.api-key=835464144378647
cloudinary.api-secret=rBFENPLExvmVFEN77p0Por6tjTs

# Admin defaults
admin.email=admin@sbtet.telangana.gov.in
admin.password=Admin@123456

# Attendance gap hours (6 hours for full day)
# Multipart / JSON limit
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB
spring.mvc.async.request-timeout=30000

# CORS allowed origin (React frontend)
cors.allowed-origin=http://localhost:3000

# Logging
logging.level.com.sbtet=INFO
logging.level.org.springframework.security=WARN

spring.jpa.open-in-view=true
# ??? Attendance Settings ????????????????????????????????????????????????????

# Hours gap between check-in and check-out needed for full attendance
attendance.gap.hours=6

# SBTET Diploma: total working days per semester (fixed denominator)
# This is what makes 1 present day = 1/90 = 1.11%  (NOT 100%)
# Change to 75 or 80 if your college has fewer working days
attendance.semester.working.days=90


spring.security.user.name=sanath
spring.security.user.password=1234